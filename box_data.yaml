# 明确指定数据集根目录的绝对路径。
# 注意：在 YAML 文件中，为了避免路径解析错误，使用正斜杠 '/' 是最安全的方式。
path: D:/a/robot/yolo/YOLOv8s/box_dataset

# train 和 val 路径是相对于上面的 path 而言的，所以它们不需要改变。
train: images/train/RGB
val: images/val/RGB

# 类别信息
nc: 1
names: ['file']